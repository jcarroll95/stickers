{
  "name": "stickerboards",
  "private": true,
  "version": "1.0.1",
  "description": "Stickerboards monorepo (API + Web) with shared tooling and documentation",
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm -w stickerboards-api run dev\" \"npm -w stickerboards-web run dev\"",
    "dev:api": "npm -w stickerboards-api run dev",
    "dev:web": "npm -w stickerboards-web run dev",

    "start:api": "npm -w stickerboards-api run start",

    "build:web": "npm -w stickerboards-web run build",
    "build:api": "npm -w stickerboards-api run build --if-present",
    "build": "npm run build:api && npm run build:web",

    "test:api": "npm -w stickerboards-api run test",
    "test:web": "npm -w stickerboards-web run test -- --run",
    "test": "npm run test:api && npm run test:web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9"
  }
}
